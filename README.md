# created a folder
# pushed(cmd) it into git hub
# changed smth locally
# again we pushed it into git hub
# created a new file (again)
# used pull(cmd) it in system
# again push(cmd) it
# clon'ed another git hub file locally
# again push(cmd) it
# trying to upload others git hub data into mine(repository)
